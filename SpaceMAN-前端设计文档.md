# SpaceMAN 前端设计文档

## 1. 文档目标

本前端面向 SpaceMAN 这一“面向巨型星座的新型智能运维底座”，目标不是做单纯的可视化大屏，而是把空间网络的运维、协同、审计和验证能力统一到一个可持续演进的工作台中。

前端需要支持四类核心场景：

1. 观测：统一查看星座、链路、算力、告警和 Agent 状态。
2. 决策：支持协调控制、协同推理、审批和工作流编排。
3. 执行：支持故障注入、卫星操作、模型配置和任务下发。
4. 验证：支持数字孪生、HITL、黑板回放和审计追踪。

## 2. 技术栈与约束

当前项目已采用以下技术组合：

- Vue 3 + TypeScript + Vite
- Vue Router（`createWebHashHistory`）
- Pinia
- Element Plus
- ECharts
- Cesium + vite-plugin-cesium
- Three.js
- Axios + WebSocket
- Electron 打包支持

设计上需要遵守的约束：

- 路由以桌面工作台为主，优先支持单页内多视图切换。
- 高风险操作必须显式确认，且保留审批轨迹。
- 3D 地球/卫星视图必须全屏可用，不放入卡片容器中。
- 仪表盘、地图、编辑器、Agent 面板需要共享同一套状态源。

## 3. 信息架构

当前路由结构已经可以作为前端信息架构的基础：

| 路由 | 页面 | 定位 |
|---|---|---|
| `/` | Dashboard | 总览态势、关键指标、全局健康度 |
| `/earth` | EarthView | 三维卫星群视图与轨道态势 |
| `/editor` | VisualEditor | 卫星参数编辑与轨道配置 |
| `/instances` | InstanceManagement | 节点/实例管理 |
| `/links` | LinkManagement | 星间/星地链路管理 |
| `/agent` | AgentChat | 协同控制台与对话式运维 |
| `/dag-pipeline` | Workflows | 运维工作流编排与执行 |
| `/blackboard` | Blackboard | 共享黑板与结构化态势快照 |
| `/faults` | Faults | 故障注入、回放与处置 |
| `/satellite/:id` | SatelliteDetail | 单星详情与操作面板 |
| `/security` | SecurityAudit | 审计与权限视图 |
| `/llm` | LlmStatus | 模型状态与配置管理 |

## 4. 全局页面壳

`App.vue` 已经定义了统一工作台框架，建议保持为主壳，不再拆成多个独立入口。

### 4.1 布局结构

- 左侧：导航栏，按“总览 / 节点与链路 / 智能协作 / 运行控制”分组。
- 中间：主工作区，承载当前路由页面。
- 右侧：智能助手抽屉，用于快速问答和辅助排障。
- 顶部：面包屑/页面标题 + 快速打开助手按钮。
- 弹层：登录对话框、审批对话框、详情抽屉、确认框。

### 4.2 全局交互

- 主题切换：浅色 / 深色双主题，保存到 `localStorage`。
- 登录态：启动时自动读取本地认证信息；未登录时弹出登录框。
- 助手抽屉：支持对当前运维问题发起快速对话，并返回建议、待审批项和结构化结果。
- 路由保护：审计页等敏感功能根据角色限制访问。

## 5. 页面设计

### 5.1 Dashboard

定位为“全局态势入口”，用于回答三个问题：当前星座是否健康、哪里拥塞、哪里需要人工介入。

建议内容：

- 顶部四张关键指标卡：星座规模、链路吞吐、算力负载、控制时延。
- 左侧图表：异构层级分布、节点数量统计。
- 中间图表：链路吞吐/控制面时延趋势。
- 右侧图表：健康度分布与异常占比。
- 入口按钮：跳转至三维地球视图。

### 5.2 EarthView

定位为“空间态势主视图”，以 Cesium 地球为核心，不附加多余壳层。

建议能力：

- 全屏地球与轨道渲染。
- 点击卫星选中后展示侧边详情。
- 支持状态筛选、轨迹高亮、镜头聚焦。
- 与 `satelliteStore` 联动，实时同步选中状态。

### 5.3 VisualEditor

定位为“卫星参数与轨道编辑器”，用于调试卫星对象和验证模型。

建议能力：

- 左侧卫星列表。
- 右侧详情卡片或编辑表单。
- 支持新增、编辑、删除、恢复默认轨道。
- 编辑内容包括名称、状态、经度、倾角、相位、高度。

### 5.4 AgentChat

定位为“协同控制台”，是 SpaceMAN 的核心操作页。

建议分区：

- 状态总览：Coordinator、Specialists、Edge Agents、待审批数、A2A 协商数。
- 对话输入：选择 Agent 类型，输入运维任务，生成 Trace。
- 消息流：展示响应、建议和动作摘要。
- Trace 区：展示执行计划、阶段状态和结果。
- Blackboard 区：展示结构化共享状态。
- 审批区：高风险动作的通过/驳回。
- LLM 配置区：模型、端点、连通性测试。

### 5.5 InstanceManagement / LinkManagement / Workflows

这三类页面适合做成“资源台账 + 执行面板”的组合页。

建议内容：

- 列表：支持筛选、搜索、批量操作。
- 详情：展示配置、状态、资源和上下游关系。
- 操作：启动、停止、编排、解绑、执行、撤销。
- 状态：加载中、空状态、异常态、只读态。

### 5.6 Faults / SecurityAudit / LlmStatus / Blackboard

这组页面属于“运维支撑层”。

- Faults：故障模板、注入记录、恢复结果。
- SecurityAudit：审计日志、敏感操作、角色边界。
- LlmStatus：模型在线状态、端点、配置验证。
- Blackboard：Trace 相关的结构化事实快照和回放。

## 6. 核心数据流

### 6.1 状态管理

现有 Pinia Store 建议保持以下职责边界：

- `authStore`：登录态、角色、本地用户信息。
- `satelliteStore`：卫星编排、选中态、位置与运行状态。
- `instanceStore`：节点实例。
- `linkStore`：链路资源和启停状态。
- `emulateStore`：仿真配置与运行开关。
- `anomalyStore`：异常事件与分析结果。

### 6.2 API 分层

建议按业务域封装：

- `agentApi`：Agent 状态、对话、任务。
- `satopsApi`：Trace、Blackboard、审批、A2A 协商。
- `llmApi`：模型状态、配置、连通性测试。
- `faultApi`：故障注入和恢复。
- `satelliteApi` / `godNodeApi`：单星控制和物理动作。

### 6.3 实时性

- 仪表盘和状态页采用轮询刷新。
- 核心运维态建议补充 WebSocket 订阅。
- 地图和编辑器依赖本地状态，减少重渲染。

## 7. 视觉规范

建议采用“深空运维台”风格：

- 背景：深蓝黑和浅灰双主题。
- 主色：蓝色表示控制与链接，绿色表示健康，橙色表示告警，红色表示高风险。
- 面板：低圆角、轻阴影、少装饰。
- 图表：强调可读性，不追求过度炫光。
- 3D 视图：采用全屏沉浸式背景，不叠多层卡片。

## 8. 组件规范

建议优先抽象以下公共组件：

- `AppShell`：全局布局壳。
- `SideNav`：分组导航。
- `TopBar`：标题和快捷操作。
- `AgentDrawer`：智能助手抽屉。
- `StatCard`：关键指标卡。
- `StatusBadge`：状态标签。
- `TracePanel`：Trace 和 Blackboard 展示。
- `ConfirmActionDialog`：高风险操作确认。

## 9. 落地顺序

### 第一阶段

- 稳定 `App.vue` 全局壳。
- 完成 Dashboard、EarthView、VisualEditor。
- 接通登录态、主题切换、助手抽屉。

### 第二阶段

- 完成 AgentChat 的 Trace、审批、Blackboard、LLM 配置闭环。
- 补齐 InstanceManagement、LinkManagement、Workflows。
- 接入故障注入与审计页。

### 第三阶段

- 接入 WebSocket 实时数据流。
- 强化单星操作和 God Node 控制。
- 补齐 HITL、数字孪生和验证视图。

## 10. 验收标准

前端达到以下状态即可认为可交付：

1. 能登录、切换主题、退出登录。
2. 能在总览、三维地球、编辑器之间稳定切换。
3. 能发起 Agent 任务并看到 Trace、Blackboard、审批结果。
4. 能查看卫星、链路、故障、审计和模型状态。
5. 能在低分辨率下保持布局不崩坏。
6. 能对接后端 API 和本地 mock 服务，不依赖手工改码运行。

## 11. 结论

SpaceMAN 前端应被定义为“统一运维工作台”，而不是几个孤立页面的集合。它的核心价值在于把三维态势、Agent 协同、审批审计、故障处置和模型配置放进同一条操作链路中，形成面向巨型星座的可视、可控、可追踪前端底座。
